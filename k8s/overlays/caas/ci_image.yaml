apiVersion: apps/v1
kind: Deployment
metadata:
  name: humancellatlas-project-catalogue
spec:
  template:
    spec:
      containers:
        - name: humancellatlas-project-catalogue
          image: $IMAGE_NAME
